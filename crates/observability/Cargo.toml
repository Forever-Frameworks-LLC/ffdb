[package]
name = "ffdb-observability"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
prometheus = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"
uuid = { version = "1", features = ["v7"] }
zeroize = "1.8"

